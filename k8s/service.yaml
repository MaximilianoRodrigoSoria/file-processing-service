apiVersion: v1
kind: Service
metadata:
  name: file-processing-service-service
  namespace: file-processing-service
  labels:
    app.kubernetes.io/name: file-processing-service
spec:
  type: ClusterIP        # El Ingress (ingress.yaml) enruta el tráfico externo al Service
  selector:
    app: file-processing-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
